import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Before I Kick The Bucket',
  webDir: 'www'
};

export default config;
